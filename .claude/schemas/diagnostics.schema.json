{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "diagnostics.schema.json",
  "title": "Psychology Diagnostics Frontmatter",
  "description": "psychology/diagnostics.md — base profile frontmatter constrained to domain=psychology. Big-Five (1-10) + ICD-11 (0-100) numeric ranges live in markdown tables, validated heuristically (psy:health-check), not frontmatter-enforceable.",
  "allOf": [
    { "$ref": "profile-frontmatter.schema.json" },
    { "properties": { "domain": { "const": "psychology" } } }
  ]
}
