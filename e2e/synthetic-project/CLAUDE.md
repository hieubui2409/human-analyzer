# Synthetic e2e fixture project

A fully SYNTHETIC two-character corpus (test-alpha, test-beta) used to exercise the 6 frameworks
end-to-end. No real PII. Point a run at it with `PMC_PROJECT_ROOT=$(pwd)/e2e/synthetic-project`.
